import styled from "styled-components";
import { BLUE01 } from "./Variables";

export const CustomButton = styled.button`
  cursor: pointer;
  color: #fff;
  font-size: 16px;
  line-height: 24px;
  font-weight: 700;
  height: 64px;
  border: none;
  border-radius: 8px;
  background-color: ${BLUE01};
`;
